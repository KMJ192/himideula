import type { RadioGroupOption } from '@ssamssam/react-ui';

const TOGGLE: Array<RadioGroupOption> = [
  {
    key: 0,
    children: 'true',
  },
  {
    key: 1,
    children: 'false',
  },
];

const OPTIONS = {
  TOGGLE,
};

export { OPTIONS };
