import type { RadioGroupOption } from '@upcast/react-ui';

const TOGGLE: Array<RadioGroupOption> = [
  {
    key: 0,
    children: 'false',
  },
  {
    key: 1,
    children: 'true',
  },
];

const OPTIONS = {
  TOGGLE,
};

export { OPTIONS };
