import type { RadioGroupOption } from '@ssamssam/react-ui';

const DIRECTION: Array<RadioGroupOption> = [
  {
    key: 0,
    children: 'horizontal',
  },
  {
    key: 1,
    children: 'vertical',
  },
];

const OPTIONS = {
  DIRECTION,
};

export { OPTIONS };
